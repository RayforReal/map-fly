export type Options ={
    element:HTMLElement
}

export type IData = {
    from: {
        id: number|string
        lon: number
        lat: number
    },
    to: {
        id: number|string
        lon: number
        lat: number
    }
}
